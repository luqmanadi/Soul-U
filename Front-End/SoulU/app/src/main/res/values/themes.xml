<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SoulU" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/forest_frolic</item>
    </style>

    // Thema Splash Screen
    <style name="Theme.App.SplashScreenSoul_U" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/forest_frolic</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_splashscreen</item>
        <item name="postSplashScreenTheme">@style/Theme.SoulU</item>
    </style>

</resources>